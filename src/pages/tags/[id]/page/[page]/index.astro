---
import type { GetStaticPaths } from "astro";
import { paths } from "./_getStaticPaths";
import Page from "./_page.astro";

export const getStaticPaths = (async ({ paginate }) => {
	return await paths(paginate);
}) satisfies GetStaticPaths;
---

<Page {...Astro.props} />
