---
import type { GetStaticPaths } from "astro";
import { paths } from "@/pages/tags/[id]/_getStaticPaths";
import Page from "@/pages/tags/[id]/_page.astro";

export const getStaticPaths = (async () => {
	return await paths(true);
}) satisfies GetStaticPaths;
---

<Page {...Astro.props} />
